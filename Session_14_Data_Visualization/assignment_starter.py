import pandas as pd
import matplotlib.pyplot as plt
import seaborn as sns

# TODO: 1. Load the data from 'weather_data.csv' using Pandas
# df = ...

# TODO: 2. Create a figure with 2x2 subplots
# fig, axes = plt.subplots(nrows=2, ncols=2, figsize=(12, 10))
# fig.suptitle("Monthly Weather Analysis", fontsize=16)

# TODO: 3. Populate the Top-Left Subplot (axes[0, 0]) -> Line Plot: Day vs Temperature
# axes[0, 0].plot(...)
# Add title and labels

# TODO: 4. Populate the Top-Right Subplot (axes[0, 1]) -> Scatter Plot: Humidity vs WindSpeed
# sns.scatterplot(ax=axes[0, 1], ...)
# Add title

# TODO: 5. Populate the Bottom-Left Subplot (axes[1, 0]) -> Bar Chart: Weather Conditions Count
# condition_counts = df['Condition'].value_counts()
# axes[1, 0].bar(...)
# Add title and labels

# TODO: 6. Populate the Bottom-Right Subplot (axes[1, 1]) -> Histogram: Temperature Distribution
# axes[1, 1].hist(...)
# Add title and labels

# TODO: 7. Adjust layout and display
# plt.tight_layout()
# plt.show()
